---
# Role metadata. The galaxy_info block exists to satisfy ansible-lint's
# role-name / meta-no-info rules, not to publish to Galaxy (this repo is
# consumed via a sibling checkout - see the repo README).

galaxy_info:
  role_name: artifact_report
  author: Common-Ansible
  description: >-
    Render where each toolchain's artifact was fetched from and where it
    resides on the VM now - source URL, cache path, probed size and mtime,
    and the unpacked directory. Reads the artifact_report_entries
    accumulator; installs nothing itself.
  license: MIT
  min_ansible_version: "2.18"
  platforms:
    - name: Ubuntu
      versions:
        - noble
  galaxy_tags:
    - system
    - toolchain

# Standalone: the role reads one accumulator var and renders it. It has no
# meta dep on the roles that produce the entries - a play that includes it
# without them reports an empty run, which is itself informative.
dependencies: []
